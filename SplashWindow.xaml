<Window x:Class="PawCraft.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Splash" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Width="400" Height="300"
        WindowStartupLocation="CenterScreen">
    <Border Background="#2D2D30" CornerRadius="20" BorderBrush="#ff6b81" BorderThickness="2">
        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🐾" FontSize="40" HorizontalAlignment="Center"/>
                <TextBlock Text="PawCraft" FontSize="24" FontWeight="Bold" 
                          Foreground="#ff6b81" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Margin="0,20,0,0"
                            Foreground="#ff6b81"/>
                <TextBlock x:Name="LoadingText" Text="Initializing..." Foreground="White" 
                          HorizontalAlignment="Center" Margin="0,10,0,0" FontStyle="Italic"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
